<div class="book-section">
  
    <div class="book-text">
        <h2>Mød vores Tryllekunstnere</h2>
        <h3>TrylleStjernerne består af i alt syv forskellige tryllekunstnere.</h3>
    </div>
      <div class="book-glowpic">
        <div class="bookpic">
            <img src="./images/book_img.webp" alt="Book">
        </div>
        <!-- <img class="glow-stars" src="./images/stars_master.webp" alt="Glowing stars"> -->
    </div>
</div>


<style>
.book-section {
    display: flex; /* Enables side-by-side layout */
    justify-content: space-between; /* Adds space between columns */
    align-items: center; /* Vertically aligns items */
    position: relative; /* Allows the background to be positioned */
    padding: 2rem; /* Adds padding around the content */
    /* Light background for better visibility */
}

.book-glowpic {
    flex: 1; /* Makes this section take up equal space */
    display: flex; /* Ensures the image centers within the column */
    justify-content: center; /* Horizontally centers the image */
    align-items: center; /* Vertically centers the image */
    position: relative; /* For star background positioning */
}

.bookpic img {
    max-width: 100%; /* Ensures the image is responsive */
    height: auto; /* Maintains aspect ratio */
    z-index: 2; /* Keeps the image above the background */
}

.book-text {
    flex: 1; /* Makes this section take up equal space */
    padding: 2rem; /* Adds space around the text */
    text-align: left; /* Aligns the text to the left */
    position: relative; /* For glow background positioning */
    z-index: 2; /* Keeps text above the background */
}

.book-text h2 {
    font-size: 4.5rem;
    margin-bottom: 1rem;
    color:rgba(189, 123, 42, 1); /* Dark text for contrast */
    padding-right: 14rem;
}

.book-text h3 {
    font-size: 1.5rem;
    color: rgba(189, 123, 42, 1); /* Slightly lighter text for subheading */
    padding: 5px 5px;
}


/* Container styles */
.book-section {
    position: relative; /* Enables the use of ::after */
    display: flex; /* Creates a two-column layout */
    justify-content: space-between; /* Aligns content on either side */
    align-items: center; /* Centers content vertically */
    padding: 2rem; /* Adds spacing */
    height: 100vh; /* Full viewport height */
    width: 100%; /* Full width */
    overflow: hidden; /* Prevents content from spilling out */
}

/* Glowing star background */
.book-section::after {
    content: '';
    position: absolute;
    top: -10%; /* Allows the image to extend slightly upward */
    right: -10%; /* Moves the background off-screen slightly */
    width: 60%; /* Covers a wider portion of the right side */
    height: 130%; /* Extends height to maintain aspect ratio */
    background: url('./images/stars_master.webp') no-repeat;
    background-size: contain; /* Maintains the aspect ratio of the image */
    background-position: center; /* Centers the background */
    opacity: 0.6; /* Adjusts transparency */
    z-index: 0; /* Places it behind content */
    pointer-events: none; /* Prevents interaction with the background */
}

/* Book image styles */
.book-glowpic {
    position: relative; /* Keeps it above the background */
    z-index: 2; /* Higher than the background */
    width: 40%; /* Adjusts width for layout balance */
}

.book-glowpic img {
    width: 100%; /* Makes the image fully responsive */
    height: auto; /* Maintains aspect ratio */
}

/* Text content styles */
/* Container styles */
.book-section {
    position: relative; /* Enables the use of ::after */
    display: flex; /* Creates a two-column layout */
    justify-content: space-between; /* Aligns content on either side */
    align-items: center; /* Centers content vertically */
    padding: 2rem; /* Adds spacing */
    height: 100vh; /* Full viewport height */
    width: 100%; /* Full width */
    overflow: hidden; /* Prevents content from spilling out */
}

/* Glowing star background */
.book-section::after {
    content: '';
    position: absolute;
    top: -10%; /* Allows the image to extend slightly upward */
    right: -10%; /* Moves the background off-screen slightly */
    width: 60%; /* Covers a wider portion of the right side */
    height: 130%; /* Extends height to maintain aspect ratio */
    background: url('./images/stars_master.webp') no-repeat;
    background-size: contain; /* Maintains the aspect ratio of the image */
    background-position: center; /* Centers the background */
    opacity: 0.6; /* Adjusts transparency */
    z-index: 0; /* Places it behind content */
    pointer-events: none; /* Prevents interaction with the background */
}

/* Book image styles */
.book-glowpic {
    position: relative; /* Keeps it above the background */
    z-index: 2; /* Higher than the background */
    width: 40%; /* Adjusts width for layout balance */
}

.book-glowpic img {
    width: 100%; /* Makes the image fully responsive */
    height: auto; /* Maintains aspect ratio */
}

/* Text content styles */
.book-text {
    position: relative; /* Keeps text above the background */
    z-index: 2; /* Higher than the background */
    width: 50%; /* Adjusts width for layout balance */
    color: white; /* Makes the text readable */
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.7); /* Enhances contrast */
    padding: 1rem; /* Adds internal spacing */
}

.book-text h2, .book-text h3 {
    margin: 1rem 0;
}

/* Responsive adjustments */
@media (max-width: 768px) {
    .book-section {
        flex-direction: column; /* Stacks content vertically */
        text-align: center; /* Centers content */
        height: auto; /* Adjusts height for smaller screens */
    }

    .book-section::after {
        width: 100%; /* Covers the entire section */
        height: 100%; /* Adjusts height for smaller screens */
        top: 0;
        right: 0;
        opacity: 0.3; /* Reduces intensity for smaller screens */
    }

    .book-glowpic, .book-text {
        width: 100%; /* Full width for stacked layout */
    }

    .book-text {
        padding: 1rem; /* Adds consistent spacing */
    }
}


.book-text h2, .book-text h3 {
    margin: 1rem 0;
}

/* Responsive adjustments */
@media (max-width: 768px) {
    .book-section {
        flex-direction: column; /* Stacks content vertically */
        text-align: center; /* Centers content */
        height: auto; /* Adjusts height for smaller screens */
    }

    .book-section::after {
        width: 100%; /* Covers the entire section */
        height: 100%; /* Adjusts height for smaller screens */
        top: 0;
        right: 0;
        opacity: 0.3; /* Reduces intensity for smaller screens */
    }

    .book-glowpic, .book-text {
        width: 100%; /* Full width for stacked layout */
    }

    .book-text {
        padding: 1rem; /* Adds consistent spacing */
    }
}


/* Responsive Adjustments */

</style>
